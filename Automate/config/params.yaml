static_tf:
  ros__parameters:
    child_frames: 
      - Lidar
    parent_frame: base_link

odometry:
  ros__parameters:
    pcl2_topic: /point_cloud/point_cloud
    odom_topic: /odom
    base_link: base_link
    use_imu: false
    imu_topic: /imu/data